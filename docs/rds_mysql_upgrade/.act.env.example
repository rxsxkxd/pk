# .actrc の --env-file として任意で指定するローカル専用値。
# 実値を記載した .act.env は Git 管理しない。
AWS_PROFILE=your-local-readonly-profile
AWS_REGION=ap-northeast-1
AWS_DEFAULT_REGION=ap-northeast-1
# act コンテナ内で AWS CLI zip を読むディレクトリ（.actrc の volume mount と一致させる）。
ACT_AWSCLI_PACKAGE_DIR=/act-assets/awscli
